version: '3.8'
services:
#  web:
#    image: frontend-image
#    networks:
#      - PSC_TestNet
  app:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - PSC_TestNet
  db:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    container_name: mongodb.service
    networks:
      - PSC_TestNet

networks:
  PSC_TestNet:
#    external: true